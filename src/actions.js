import { createAction } from 'redux-actions'

export const fileProcessingStarted = createAction('FILE_PROCESSING_STARTED')
export const fileProcessingFinished = createAction('FILE_PROCESSING_FINISHED')
